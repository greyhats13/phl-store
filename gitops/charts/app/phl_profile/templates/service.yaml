apiVersion: v1
kind: Service
metadata:
  name: {{ include "phl_profile.fullname" . }}
  labels:
    {{- include "phl_profile.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phl_profile.selectorLabels" . | nindent 4 }}
