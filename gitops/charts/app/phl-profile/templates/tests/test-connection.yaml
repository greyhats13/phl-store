apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phl_profile.fullname" . }}-test-connection"
  labels:
    {{- include "phl_profile.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phl_profile.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
