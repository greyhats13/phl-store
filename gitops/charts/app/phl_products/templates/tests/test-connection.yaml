apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phl_products.fullname" . }}-test-connection"
  labels:
    {{- include "phl_products.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phl_products.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
