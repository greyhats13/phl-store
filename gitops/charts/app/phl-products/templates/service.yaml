apiVersion: v1
kind: Service
metadata:
  name: {{ include "phl_products.fullname" . }}
  labels:
    {{- include "phl_products.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phl_products.selectorLabels" . | nindent 4 }}
